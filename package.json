{
  "name": "kitap-rafi",
  "version": "1.0.0",
  "description": "Scroll-driven kitap tanıtım sitesi + admin paneli",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --check server.js && node scripts/test-parse.js && bash scripts/orchestration-check.sh && bash scripts/agent-protocol-check.sh"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1"
  }
}
